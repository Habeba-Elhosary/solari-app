import 'package:flutter/material.dart';

class AddPanelScreen extends StatelessWidget {
  const AddPanelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('AddPanelScreen'),
      ),
    );
  }
}
