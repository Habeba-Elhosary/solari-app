import 'package:flutter/material.dart';

class AddSystemScreen extends StatelessWidget {
  const AddSystemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('AddSystemScreen'),
      ),
    );
  }
}
